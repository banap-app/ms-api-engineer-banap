export type CreateEngineerCommandConstructorProps = {
    name: string
    email: string
    password: string
    crea: string
    isActive: boolean
}

export class CreateEngineerCommand {
    constructor(props:CreateEngineerCommandConstructorProps) {
        if(!props) return
        
    }
}